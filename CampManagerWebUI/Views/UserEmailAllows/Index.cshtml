@model IEnumerable<CampManagerWebUI.Models.UserEmailAllowViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Dodaj nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.StartRoles)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateExpire)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.StartRoles)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateExpire)
        </td>*@
        <td>
            @*@Html.ActionLink("Edycja", "Edit", new { id=item.Id }) |*@
            @*@Html.ActionLink("Szczegóły", "Details", new { id=item.Id }) |*@
            @Html.ActionLink("Usuń", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
